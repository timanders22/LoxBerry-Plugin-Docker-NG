# These packages will be installed by apt-get -y install PACKAGENAME
# One line per package, use exact packagename as you would do for apt-get.
#
# Hinweis: "software-properties-common" wurde entfernt. Das Paket stammt aus der
# Ubuntu-Welt (liefert add-apt-repository) und ist unter Debian trixie in den
# aktivierten Quellen nicht vorhanden - apt bricht dann mit Fehler 100 ab, obwohl
# es fuer Docker gar nicht gebraucht wird.
# "apt-transport-https" ist seit apt 1.5 ein leeres Uebergangspaket; HTTPS kann
# apt von Haus aus. Beides wird von get.docker.com ohnehin nicht verlangt.
ca-certificates
curl
gnupg2
